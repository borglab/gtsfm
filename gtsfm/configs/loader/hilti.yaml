# Hilti Loader Configuration
_target_: gtsfm.loader.Hilti
dataset_dir: ??? # Required - path to Hilti dataset directory
images_dir: null # Optional - path to images directory (defaults to {dataset_dir}/images)
max_length: null # Optional - maximum number of timestamps to process
max_resolution: 1080
input_worker: null # optional input worker specification
